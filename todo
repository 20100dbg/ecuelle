Proxy

- edit handshake to force disable SSL

- loop on recv if len(data) == max data

- execute arbitrary query at any time ?

- check client capabilities

- sql color syntax : https://github.com/scriptcoded/sql-highlight
-- format SQL
-- replace ? with params
-- delete /**/ comments + trim()

- TLS support

- mysql PACKET_OK
-- handle len(payload) > 7

- mysql
-- test return many rows
-- test named parameters

- option log
-- print
-- write file

- display table, columns :
-- timestamp
-- IP:port
-- affected_rows / len(rows)
-- state (ok / error)
-- show original / pretty query
-- actions
--- ignore query
---- as is
---- with these parameters
--- 

Known bugs :
- with mysql, classic query, if first field is empty (select ""), response is detected as prepared statement, resulting in crash


# postgresql
https://docs.postgresql.fr/14/protocol.html

# MSSQL
# 2025, 2022, 2019, 2017
docker pull mcr.microsoft.com/mssql/server:2025-latest

